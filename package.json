{
  "name": "backend-phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../phonebook-project && npm run build && cp -r dist ../backend"
  },
  "author": "Faris",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "mongoose": "^9.2.1",
    "morgan": "^1.10.1"
  }
}
